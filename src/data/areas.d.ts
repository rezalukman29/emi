export interface AreaData {
  id: number;
  name: string;
  desc: string;
  createdAt: string;
  updatedAt: string;
}

export const SUB_AREAS: Record<string, string[]>;
export const initialAreas: AreaData[];
